export const selectCourses = (state) => state.courses;
export const selectAuthors = (state) => state.authors;
export const selectLoading = (state) => state.system.isLoading;
export const selectUser = (state) => state.user;
